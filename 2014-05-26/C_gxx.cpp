#include <cstdio>
#include <cstring>
#include <map>

const int N = 150 + 1;

int x_min, x_max, a_min, a_max, y_min, y_max, b_min, b_max, z_min, z_max, c_min, c_max;

int main() {
    freopen("equality.in",  "r", stdin);
    //freopen("equality.out", "w", stdout);
    scanf("%d%d", &x_min, &x_max);
    scanf("%d%d", &a_min, &a_max);
    scanf("%d%d", &y_min, &y_max);
    scanf("%d%d", &b_min, &b_max);
    scanf("%d%d", &z_min, &z_max);
    scanf("%d%d", &c_min, &c_max);
    return 0;
}
